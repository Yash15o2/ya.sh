import { TextTypingDirective } from './text-typing.directive';

describe('TextTypingDirective', () => {
  it('should create an instance', () => {
    const directive = new TextTypingDirective();
    expect(directive).toBeTruthy();
  });
});
