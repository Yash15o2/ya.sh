import { StretchableDirective } from './stretchable.directive';

describe('StretchableDirective', () => {
  it('should create an instance', () => {
    const directive = new StretchableDirective();
    expect(directive).toBeTruthy();
  });
});
