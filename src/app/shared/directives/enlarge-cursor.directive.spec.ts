import { EnlargeCursorDirective } from './enlarge-cursor.directive';

describe('EnlargeCursorDirective', () => {
  it('should create an instance', () => {
    const directive = new EnlargeCursorDirective();
    expect(directive).toBeTruthy();
  });
});
