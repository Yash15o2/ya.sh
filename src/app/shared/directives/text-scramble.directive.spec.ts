import { TextScrambleDirective } from './text-scramble.directive';

describe('TextScrambleDirective', () => {
  it('should create an instance', () => {
    const directive = new TextScrambleDirective();
    expect(directive).toBeTruthy();
  });
});
